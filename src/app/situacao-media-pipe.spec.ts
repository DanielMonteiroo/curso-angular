import { SituacaoMediaPipe } from './situacao-media-pipe';

describe('SituacaoMediaPipe', () => {
  it('create an instance', () => {
    const pipe = new SituacaoMediaPipe();
    expect(pipe).toBeTruthy();
  });
});
